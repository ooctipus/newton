# FPGS structural reopening, 2026-09-11

This continues the [inherited study and handoff](LARGE_GAIN_STUDY_20260911.md)
on `ooctipus/fpgs-redo-20260911`, starting at Newton
`fe484a0a58293925f889cb8c641e781569c9ae70` (accepted a7 runtime).
Isaac Lab runtime, task parameters, substeps and iteration allowances are
unchanged. Original worktrees and earlier evidence remain intact.

## Runtime change in this branch

`FEATHER_PGS_SIMPLE_WORLD_ZERO=1` enables an **experimental, opt-in** pre-row
classifier for the original Kuka23+free6 matrix-free recipe. It checks the
current predicted velocity against all applicable raw contact normals,
activated position limits and rigid speed caps. An admitted world needs no
constraint impulse, so its contact/limit allocators publish zero row counts
and fresh negative contact routes. Every other world retains the original
complete solver. Original force conversion, dynamics and integration remain.

This does not delete collision geometry, lower iteration counts, reuse stale
forces, or replace the incoming predictor. Unsupported configurations use the
original path. The selector is not a public stable API or a general solver for
all task recipes. The default is disabled.

The ordinary FP32 rejection margins are conservative fallback decisions, not
an interval-proof requirement and not permission to violate constraints.
Original restitution incident velocity, prescribed motion and current MF
versus held dense response conventions are retained.

## New measurements

Physics-graph medians, 16,384 environments, three alternating AB/BA/AB rounds,
200 warmup steps, 40 measured steps and 40 graph-profile steps. GPUs ran
concurrently, one owned job per GPU. These are not training throughput or
MJWarp accuracy-parity results.

| Task / change | RTX PRO6000 baseline → candidate | GB300 baseline → candidate |
| --- | ---: | ---: |
| Kuka / zero-row path in this branch | 17.757 → 15.375 ms; 1.155× | 17.130 → 15.367 ms; 1.115× |
| Allegro / reopened rejection-only collision candidate | 19.097 → 17.372 ms; 1.099× | 21.064 → 17.875 ms; 1.178× |

The Allegro candidate remains in the separate preserved
`/home/octi/Projects/newton-fpgs-certified-rejection-20260911` worktree; it is
not silently included in this branch's runtime change. Its corrected live512,
live16K and held-out seed/reset checks pass on both GPUs. These checks are
bounded contact-publication evidence, not a long-horizon task-success study.

The additional whole-task 2–4× target is **not achieved** by these measurements.
No new MJWarp comparison was run in this reopening.

## What the earlier “losses” actually mean

- The 66 saved warm-collision examples were geometric patch-preservation
  failures, **not 66 demonstrated physical/task failures**. The previous gate
  also incorrectly rejected unchanged baseline queries under original MPR
  semantics; its producer and depth rules have been corrected.
- A widest saved5→1 change collapses an81.7mm patch. At a hypothetical fixed1N
  normal load, the maximum unavailable normal moment is0.06585N·m. That is a
  real change in conditional support capacity, but every selected gap is
  positive and actual solved loads were not saved. It does not establish
  current harm or harmlessness. Positive gaps may still carry speculative
  impulses when approaching.
- One saved interior contact can be removed without changing its static
  normal-moment hull. That alone does not prove dynamic equivalence under
  differing gaps, compliance, friction and finite iteration.
- The row-free zero path has direct bounded physical evidence:1742/2048
  captured world-states pass independent FP64 raw-normal/limit checks, require
  no velocity correction, and publish no rows. Actual original force conversion
  passes stable-graph nonzero→zero→nonzero transitions and poisoned unread-row
  controls on both GPUs. This establishes useful avoidable work in those states.

Acceptance should concern numerical convergence, force/momentum consistency
and task-relevant behavior, not bit identity or unconditional contact count.
Same-input full/full/warm force and residual diagnostics are being completed
separately; no dynamic conclusion is inferred from patch counts alone.

## Why the theoretically sparse experiment was slow

The old private A prototype computed201 composite-inertia actions instead of29
and scanned2142 factor products although only747 sparse intersections were
nonzero. Correcting those two ownership/arithmetic mistakes produces identical
factors and predictors in full16K captures, with refresh times:

- RTX:4603.936→2485.280µs,1.852×.
- GB:3310.592→1489.376µs,2.223×.

Reuse regresses772.080→847.872µs RTX and506.304→512.480µs GB. This is a
component improvement over an earlier **slow prototype**, not a gain over
accepted whole physics. Corrected A alone still costs more than the original
full dynamics pipeline. Keep original canonical FK/mass/predictor/publication
providers while developing row-free consumers; do not require a slow A/C rewrite.

## Next large-gain ownership changes

The zero classifier costs about1.24/1.13ms per environment step in node traces,
offsetting part of the3.37/3.26ms reduction in row/response/GS node sums. These
are summed node costs, not independent critical-path attribution.

A private CPU one-bound reference raises admission to1949/2048 captured states
(207 accepted projections). Independent raw contact and bound checks remain
feasible; worst FP64 velocity error is5.95e-8. A rounded equality residual of
-2.98e-8 is consistent with ordinary roundoff, not demonstrated harmful loss.
This reference is **not installed, GPU-timed or trajectory-validated**.

The remaining mixed-world solver also uses overly coarse ownership: in four
old full16K snapshots,278/282 RTX and287/294 GB mixed worlds have uncoupled
hand/object constraint components. Most hands are zero-impulse or one-bound
candidates, yet independent object-ground contact sends all hand rows through
generic29D GS. Component-local selection plus an intact free6 solve is a
structural target. Preserve coupled-world fallback, current MF mass, force
publication and correct physical-Y versus whitened-Z representation.

## Local evidence and reproduction

Large captures stay local. Exact source/artifact hashes and commands are in:

- `/tmp/fpgs-redo-20260911-VqwICA/PLAN.md`
- `/tmp/fpgs-zero-timing-{nodes16k,whole16k}-01/`
- `/tmp/fpgs-zero-capture-enabled-512-01/`
- `/tmp/fpgs-zero-force-paired-01/`
- `/tmp/fpgs-rejection-reopen-{quality-512,quality-16k,quality-heldout,whole-16k}-01/`
- `/tmp/fpgs-kuka-a-redo16k-01/`
- `/tmp/fpgs-one-world-private-lNzNJr/CHECKPOINT.md`
- `/tmp/fpgs-mf-islands-ankoGAyD/FINDINGS.md`

The paired zero runner is
`/tmp/fpgs-redo-20260911-VqwICA/run_zero_timing.py`: run `nodes --output NEW_DIR`,
then `whole --predecessor NEW_DIR/timing_driver.json --output ANOTHER_NEW_DIR`
with the pinned original Lab interpreter. It freezes sources per run and uses
the original unchanged Lab profiling harness; source changes require a fresh
node predecessor. Set the opt-in flag before importing Newton when using the
runtime directly. Full runtime settings and GPU UUIDs are in each manifest.

One inherited contact-bias test still fails on clean a7 with a13-versus15
kernel-argument mismatch. It is not repaired or counted as passing here.
