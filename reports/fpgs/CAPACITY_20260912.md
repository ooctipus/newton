# Capacity and line-search correction — 12 September 2026

This continues the original handoff and [cross-task study](CROSS_TASK_20260911.md).
It is correctness and capacity work, **not an optimization speedup claim**. Previous
Keyboard timings with dropped rows and MJWarp timings with line-search warnings
are not an accuracy-fair baseline.

Latest validated scope: FPGS Keyboard4K has no observed lost rows/contacts with
704 rows and calibrated compact collision storage. Corrected MJ Keyboard4K
has zero warning/capacity bits and no huge/nonfinite active forces across
40,632,320 observed world-solves per GPU. The final search also clears the
remaining Franka64 failures. Kuka16K passes 162,529,280 observed world-solves per
GPU with calibrated storage. ANYmal-D also passes 424,673,280 observed
world-solves per GPU with compact storage. Allegro's friction-cost cancellation
has a separate same-input regression and paired causal GPU control. A single checked Keyboard
timing sample is recorded below, separately from instrumented diagnostics.

## Scope and provenance

- Newton branch: `ooctipus/fpgs-capacity-20260911`, fork
  `https://github.com/ooctipus/newton.git`; starting commit
  `35b19928df46465137ecc2cf92da4e6f8a8bd17b`.
- Capacity implementation checkpoint: `95047f49`; external-contact DOF-response
  correction: `5c31305d`. Both retain the original handoff ancestry/report.
- Checked capture and opt-in pyramidal line-search implementation: `360388d1`.
  Checked capture became the default in `b9aa6ce4`; historical unchecked runs
  require explicit `--allow-unchecked`. The later experimental elliptic
  extension retains the reviewed pyramidal generated source exactly.
- External-prefix guard and isolated elliptic helper: `108459ec`. All 83
  benchmark-tool CPU tests pass; all 88 capacity/response tests pass on each
  actual GPU, including the new converter graph replay, with no skips.
- Unchanged Isaac Lab checkout: `1d8feb82d17dbfab8f0772de56f84deae2cb7974`.
- RTX PRO 6000 Blackwell Max-Q and GB300 run concurrently, one child per device;
  every completed batch is reaped and source/idle guards checked.
- Warp 1.17.0, MuJoCo/MJWarp 3.12.0. Installed MJWarp solver/io/types match the
  official 3.12.0 wheel byte-for-byte; no dependency source is edited.
- Task timesteps, substeps, solver sweeps/iteration allowances and tolerances
  remain unchanged. All capacity changes occur before construction/capture.
- Instrumented controls below add diagnostic device work. Their timings are
  not performance samples. Raw artifacts remain local under `/tmp`.

## FPGS: missing rows were real

Keyboard's old 192-row dense allocation could see **615 requested rows** and
drop **423**. These are mostly three-row frictional contacts, not duplicated
keyboard position limits. The 108 independent keyboard key limits use the
existing fused path outside this allocator.

The implementation now latches dense, matrix-free, propagation, and incoming
contact-capacity failures independently of opt-in detailed telemetry. Four
int32 flags survive successful/empty steps and CUDA graph replays. They add no
new per-step kernel or host synchronization. `constraint_capacity_status()`
reads them; `check_constraint_capacity()` raises. Call at safe host observation
boundaries on the correct/joined stream. Explicit clearing acknowledges an
earlier failure; it cannot repair lost work. This is not automatic graph abort.

This API does **not** cover upstream collision queues or the separate matrix-free
unique-body cap. `NarrowPhase.buffer_capacity_status()` and
`check_buffer_capacity()` now separately retain all 13 existing narrow-phase
verification conditions in a four-byte word, using the existing verifier kernel.
Keep `verify_buffers=True` throughout. These flags do not cover soft/hydroelastic
internal storage or post-narrow owners such as body-pair reduction/contact
matching. A hash-load warning is not itself proof of lost contacts. Neither
boolean API estimates required capacity; measure unclamped demand separately.

Regression-first tests include 205 sphere-plane contacts: 640- and 768-row
solutions agree within FP32 tolerance and satisfy physical checks; 192 rows
omit a necessary contact and change motion. The initial combined **68 tests passed
on both GPUs with no skips**, including sticky row/collision status during graph
replay, compact collision storage and MJ initialization; the later response
correction expands this to 83 tests. Three broader
CPU-path failures were reproduced on the clean starting revision and are not
claimed fixed by this change.

### Keyboard, 4,096 worlds

Each seed-1/seed-2 run has 200 warmup, 1,000 measured environment steps and 40
profile steps. The 1,000-step span includes repeated episode resets. All runs
below have zero measured FPGS row drops/overflow and finite boundary states.

| Control | RTX maximum dense rows | GB300 maximum dense rows | Maximum global contacts, RTX / GB300 |
| --- | ---: | ---: | ---: |
| 640 rows, default contact pool, seed 0 (short control) | 598 | 602 | 114,109 / 115,735 |
| 640 rows, 196,608 global contacts, seed 1 | 609 | 624 | 115,666 / 114,713 |
| 704 rows, 147,456 global contacts, seed 2 | 602 | 599 | 115,972 / 116,990 |
| Same, plus 57,344 broad-phase output slots, seed 3 | 602 | 601 | 112,701 / 113,097 |

The 704-row setting retains 12.8% above the largest observed 624-row demand.
The 147,456-slot contact pool retains roughly 25% above the earlier 118,209
contact peak, while removing 99.54% of the old 31,764,480 contact slots. The
known contact-plus-routing payload reduction is about 3.53 GiB; this is **not**
net process VRAM saving because row storage also increased.

The seed-2 run uses the new sticky checker and records all 4,960 collision calls
on each GPU: every observed collision queue has zero overflow. It revealed
oversized intermediate queues: 14,864,384 broad-phase output slots versus
44,984 maximum observed demand, with similarly oversized GJK/work/query arrays.

`CollisionPipeline(..., broad_phase_output_max=N)` now permits an explicit
output bound without truncating the full immutable input pair list. Dependent
narrow-phase storage follows that bound. Defaults are unchanged; unsupported
modes/prebuilt components are rejected. Rightsizing can also change effective
split/sparse dispatch, so it must be revalidated rather than described as a
storage-only change. The **57,344-slot** seed-3 control completed all 4,960 calls
per GPU with zero row/contact/collision overflow. Broad output demand peaked at
43,910 / 43,820; the reserve is 27.5% above the earlier 44,984 maximum. Full
input traversal remained 14,864,384 pairs. Sparse GJK changed from true to false;
split GJK/MPR remained enabled.

Reachable unique Warp backing storage decreased from 2,427,150,061 to
1,479,499,505 bytes (about 904 MiB removed, with unchanged 704-row/contact
settings). This is not process VRAM or a timing claim. Matrix-free storage is
already one-slot dummy storage (~0.72 MB) despite the configured 64-row maximum;
propagation storage is absent. Large remaining body/geometry arrays are sized
by actual topology. Dense704 storage is now the principal max-driven allocation.

The unchanged Lab config rejects unknown Hydra keys. Checked benchmarks pass
the new output cap directly to Newton's constructor instead, without altering
Lab files or config objects. The checked driver validates FPGS/narrow sticky
flags and all MJ warning bits at the two existing synchronized metadata
boundaries, rejecting unsupported collision owners and preserving failed checks.

## MJWarp: two different issues

### Line search

3.12.0 exposes exhaustion that was previously silent; the corresponding
3.11-to-3.12 solver arithmetic was unchanged. The warning prints the current
15-iteration setting, not a calculated recommendation to raise it. Downgrading
or suppressing warnings would not fix the search.

A stock Kuka 64-world control records 688 / 653 affected world-solves (RTX /
GB300) out of 7,168. Outer iterations peak at 7 of 100 and no contact/row
capacity overflows occur. Disabling incremental reuse gives 651 / 655, ruling
out cached-gradient reuse as the main cause.

Captured rays identify an exact-zero derivative handling bug: `_in_bracket`
rejects a trial with derivative exactly zero. A saved GB300 ray exhausts 15
iterations in literal FP32 replay; zero-inclusive bracket and convergence tests
accept its exact stationary point in one iteration. A native CPU kernel
regression also demonstrates that the old code can discard that point and
return a nonstationary bracket endpoint.

At an earlier diagnostic stage, the helper-only correction reduced Kuka failures to
27 / 35, with unchanged iteration limits and no capacity/nonfinite failures.
That partial correction still misses convergence if zero is first accepted on
the final allowed iteration. A second cause is an unattainable derivative
threshold on the FP32 alpha grid. A captured Keyboard ray has curvature around
210: one alpha ULP changes its derivative by about 2.5e-5, while the threshold
is 1.85e-6. FP64 accumulation alone cannot make an unrepresentable alpha exist.

A diagnostic correction accepts exact stationary trials, breaks exact cost
ties by stationarity, preserves geometric progress inside a finite sign
bracket, and recognizes adjacent representable bracket endpoints. Existing
evaluation and iteration budgets are unchanged; genuine budget exhaustion
still raises the warning. Small 64-world controls, 7,168 world-solves each:

| Task | Stock line-search failures, RTX / GB300 | Corrected failures, RTX / GB300 |
| --- | ---: | ---: |
| Kuka | 688 / 653 | 0 / 0 |
| Keyboard | Stock 64-world count not captured | 0 / 0 |
| Franka | 157 / 157 | 3 / 3 |

Keyboard's earlier full-zero-only control has 195 / 189 failures. Franka's
remaining rays stalled before obtaining a sign bracket when a Newton proposal
rounded back to its endpoint; the final correction is described below. Independent
stored-force balance is finite throughout these controls, but is not an
independent constitutive-law or whole-trajectory accuracy proof. Large active
Keyboard contact forces are being traced to their actual owning rows rather
than dismissed as inactive storage.

Friction absolute-cost cancellation is separately reproducible, but must not
be conflated with the proven zero-bracket bug. The later experimental ellipse
helper addresses this additional failure; the pyramidal path remains unchanged.

### Capacity initialization

Keyboard 4K, stock MJWarp, seed 0, 200 warmup + 200 environment + 40 profile
steps: requested `njmax=384`, `nconmax=36` and 147,456 Newton contact slots.
Measured row demand peaks at **272** on each GPU and pooled contacts at
**103,551 / 103,443**. No observed capacity overflow occurs, but line-search
failures remain: **1,463,063 / 1,463,595** world-solves.

The requested contact reservation does not take effect: CPU initialization
produces 120 native contacts, then the adapter raises `nconmax` to 120 and
allocates **491,520** pooled/ Newton contacts. External collision is used in
this task. The adapter now temporarily disables native contacts during the
existing CPU forward call only for the external-collision GPU seed, restoring
the exact option flags in `finally` before device transfer. Noncontact seed
constraints remain consistent; native/CPU paths and later GPU warm-start policy
are unchanged. Seven focused initialization tests pass. A 64-world GPU smoke
test and held-out seed-1 4K run both honor the requested capacities exactly,
with CPU seed `ncon=0`, `nefc=6`.

The 4K run uses 200 warmup, 1,000 environment and 40 profile steps:

| Actual allocation | Allocated | Maximum measured demand, RTX / GB300 |
| --- | ---: | ---: |
| MJ rows per world (`njmax`) | 320 | 272 / 276 |
| Pooled MJ and Newton contacts (`nconmax=32`) | 131,072 | 106,107 / 105,437 |
| Broad-phase output | 45,056 | 37,275 / 36,462 |

All 4,960 collision calls per GPU have zero raw queue overflow; all 13 sticky
narrow flags and all MJ capacity bits remain clear. Reserves above the largest
new observed demands are 15.9%, 23.5%, and 20.9%, respectively. This establishes
observed capacity safety through these held-out resets, not a universal bound.
Reachable unique Warp backing storage is 1,430,956,127 bytes per device; this
is not process VRAM. The old 491,520-contact seed-driven allocation is no
longer required (73.3% fewer pooled slots).

That earlier run is **not accepted as warning-free timing**: its adjacent-float search
still has 38 / 34 line-search failures across 40,632,320 world-solves per GPU.
There are no outer iteration exhaustions or nonfinite solves. Independently
recomputed stored-force balance is finite/in-bounds on every observed solve.
The first remaining rays again lack a sign bracket. Unlike Franka's exact
roundback, their Newton trials move by one/four ULPs; endpoint insertion and
rounded-gradient plateaus are being investigated. They remain part of the next
correction's tests; a roundback-only fix is not yet sufficient evidence.

A captured huge active force belongs to ground versus the fixed Keyboard case,
with exactly zero Jacobian, zero response weight and `D` near 1e15. Its large
force contributes no generalized force. This proves the captured row's owner,
not that every large force is harmless. FPGS already filters nonresponsive
endpoints before allocating solver rows, so this MJ row does not explain the
FPGS 624-row high-water mark.

The external-conversion filter was incomplete: Newton exports some articulated
fixed/locked-D6 roots as mocap bodies with nonzero MuJoCo weld IDs. Testing only
`body_weldid == 0` missed them. The adapter now uses native MuJoCo's welded
ancestor DOF count, retaining a contact whenever either endpoint can respond
(and preserving the existing explicit-kinematic rule). A fixed child on a
dynamic parent remains responsive; using the leaf body's DOF count would be
incorrect. No force-magnitude/inverse-weight threshold is used, and native MJ
collision is unchanged. This removes spurious public contact forces, not
capacity-driven loss of physically responsive contacts.

Regression-first tests failed before the correction. The full **83-test suite
passes on each GPU without skips**, including real sparse solves/force
publication, repeated cached conversion, invalidation, empty/refill transitions
and CUDA graph replay. Full pre-commit passes.

### Deeper line-search acceptance regressions

Further diagnosis found a missing-sign acquisition bug: both same-sign
endpoints reject a trial across the minimum. The old no-progress termination
can therefore report convergence at a substantially incorrect point with no
warning. An actual native CPU convex two-contact example stops at `x=0.49`
instead of `1.3`, with gradient magnitude `3.51` and objective regret `2.11005`.
Its physical Newton direction and SPD smooth mass are consistent, not synthetic
warning inputs. The mirrored objective reproduces the same failure.

The diagnostic correction acquires the missing-sign endpoint from the existing
three evaluated trials, then refines the bracket. A second three-contact
counterexample requires accepting collapsed same-sign anchors (`lo_alpha ==
hi_alpha`); a strict ordering precondition otherwise falsely converges at
`1.3` instead of `2.2`. Both corrected mirrored CPU examples reach their exact
minima within FP32 accuracy using the unchanged 15-iteration budget.
Warning-free alone is demonstrably not a sufficient numerical acceptance
criterion.

The final combined correction passes Franka64 on both GPUs with **zero**
line-search/outer/capacity failures (old stock157, partial fixes3 per GPU).
The full held-out Keyboard4K seed-1 run repeats the 200/1,000/40 sampling and
all-call observers, retaining `njmax=320`, `nconmax=32`, 131,072 Newton contacts
and 45,056 broad-output slots:

| Final observed demand | RTX | GB300 |
| --- | ---: | ---: |
| Maximum MJ rows per world | 256 | 260 |
| Maximum converted MJ contacts | 96,398 | 95,574 |
| Maximum raw Newton contacts | 106,108 | 105,438 |
| Maximum broad output | 37,275 | 36,462 |
| Line-search / outer / capacity failures | 0 / 0 / 0 | 0 / 0 / 0 |
| Active force observations at or above 1e12 | 0 | 0 |

Every one of the 40,632,320 world-solves per GPU is observed. All active forces
are finite; all raw queue checks, sticky flags and independent stored-force
balance metadata pass ("in-bounds" refers to indexed metadata, not a residual
tolerance). The force census checks every active force, not just the
first captured row. Its Jacobian metadata check applies only to huge-force
rows. No huge row exists in this run after the response fix.

Independent stationarity is measured, not equated with exact convergence:
RTX's final maximum rescaled stored-force gradient is 5.6367e-4 (previous
partial-correction control5.6369e-4), with peak2.0694e-3 (previous1.9669e-3).
MuJoCo also permits its existing improvement-based termination. These values
are not an independent constitutive-law, same-trajectory or FPGS/MJ accuracy
parity certificate. No tolerances or iteration limits were increased.

The corresponding GB300 final maximum is 8.8544e-4 (previous8.1826e-4), with
peak1.6249e-3 (previous1.6817e-3). These are explicit measurements rather than
a claim that every residual decreased under changed trajectories.

The final diagnostic uses Newton `5c31305d` **plus** private search factory
`65e08b064d942ffd3041d5a8f88aad5261c2c3387707b7432a09ce2bfc714e74`.
That Newton checkpoint alone does not install a line-search correction. The
first promoted pyramidal-only helper is SHA256
`c5f0a6aef071e9d47baf3c646d1f8bfe715b3a31f6a079f9b244b09f080e368c`;
its generated mathematical AST is identical to the tested private factory
after removing instrumentation and normalizing the private factory name.
Twelve self-contained CPU regressions cover both mirrored convex examples,
source/cache guards, unsupported models and genuine budget exhaustion.

### Kuka16K capacity calibration

The 64-world maximum of 39 MJ rows did not bound the 16K tail. A 64-row trial
at seed 1 with 200 warmup/1,000 environment/40 profile steps saw maximum raw
demand 66 / 67 and overflowed 4 / 2 world-solves. It is explicitly **rejected**;
diagnostic `complete/success=true` only means recording completed. Both the
independent-gradient and force-census metadata checks record those same 4 / 2
invalid observations. No timing from this run is accepted.

The repeat uses 84 rows, `nconmax=19` / 311,296 contacts, and 442,368 broad-output
slots. It passes all 162,529,280 observed world-solves and 4,960 collision calls
per GPU with zero warning/capacity/nonfinite/huge-force observations. All
independent-gradient metadata checks and narrow sticky flags pass. Maximum
rows are 66 / 71; maximum raw Newton contacts 247,032 / 245,391; broad output
354,727 / 353,423. Reserves above these larger observed maxima are 18.3%, 26.0%
and 24.7%. Converted MJ contact peaks are 181,496 / 179,855, but the upstream
Newton pool must still hold the larger raw prefix; sizing only from converted
counts would be incorrect in this Lab integration.

These are validated observed bounds for the stated workload, not universal
future limits. Rows decreased from 300 to 84 and the per-world pooled reservation
from 200 to 19, while retaining all observed responsive rows. The earlier
64-row truncated trajectory does not define the repaired run's high-water mark.

### Remaining compact MJ workloads

Franka's first 40-row 16K trial overflowed 65 / 26 world-solves and is rejected.
The repaired run uses 56 rows, `nconmax=1`, 7,680 broad-output slots and a
2,048-contact pipeline request. The unchanged public export contract actually
allocates 16,384 public Newton/MJ contacts: the smaller pipeline request is
not a 2,048-slot public allocation or an equivalent memory saving.

At held-out seed 1, 200 warmup, three repeats of 1,000 environment steps and
40 profile steps, Franka passes **424,673,280 world-solves per device**.
Maximum rows are 45 / 47; raw contacts 1,635 / 1,652; broad pairs 6,109 / 5,959.
Line-search, outer, row/contact, other solver and nonfinite counters are zero;
all narrow flags and stored-force metadata checks pass. The 56-row reserve is
19.1% over the larger new peak. One earlier 56-row attempt failed while loading
a diagnostic sibling module before physics; it is preserved as an
instrumentation failure, not a solver result. The corrected wrapper restores
its verified import path on exit and passes source/ownership checks.

The [compact elliptic audit](COMPACT_ELLIPTIC_CAPACITY_20260912.md) and its
hashed JSON preserve the corresponding Allegro/ANYmal runs, each with the same
424,673,280-world-solve exposure per GPU. Both pass observed warning/capacity
checks. A compact native Allegro trial was correctly rejected despite its
contact count fitting: native collision pairs overflowed the shared pool.
The accepted retry measures **raw pairs**, not just final contacts.

| Task / worlds | MJ rows | `nconmax` / actual global MJ pool | Newton pipeline / public contacts | Broad output |
| --- | ---: | ---: | ---: | ---: |
| Keyboard / 4,096 | 320 | 32 / 131,072 | 131,072 / 131,072 | 45,056 |
| Kuka / 16,384 | 84 | 19 / 311,296 | 311,296 / 311,296 | 442,368 |
| Franka / 16,384 | 56 | 1 / 16,384 | 2,048 / 16,384 | 7,680 |
| ANYmal-D / 16,384 | 32 | 9 / 147,456 | 147,456 / 147,456 | 294,912 |
| Allegro / 16,384 | 112 | 22 / 360,448 | Native collision | Shared native pool 360,448 |

These are calibrated observations with reserve, not mathematical universal
maxima or globally minimal allocations. Allegro raw pairs peak at 271,440 /
271,457; its contact-only peaks 153,295 / 153,328 cannot independently determine
the shared pool. ANYmal rows peak at 24 / 25, contacts 119,614 / 119,615 and
broad output 218,169 / 217,794. Coupled queue layouts and the Franka public
allocation floor are reported honestly instead of treating requested maxima
as actual memory. FPGS's separate 704-row Keyboard recipe is given above;
this table does not silently calibrate other FPGS recipes.

### Checked Keyboard4K timing sample

`/tmp/fpgs-checked-keyboard4096-20260912-01` runs the committed `360388d1`
entrypoint and final helper `c5f0a6ae`, with no extra diagnostic kernels. Nsight
graph tracing remains enabled. Both backends pass both existing metadata
boundaries on both GPUs: zero supported capacity flags, zero MJ warning bits,
finite boundary states, unchanged source guards and unchanged task budgets.
The recipe is seed 0, 200 warmup / 1,000 environment / 40 profile steps, one
round, with the compact capacities in the reproduction command below.

| GPU | FPGS physics graph | Corrected MJ physics graph | MJ / FPGS | FPGS / MJ environment wall time |
| --- | ---: | ---: | ---: | ---: |
| RTX PRO 6000 | 11.186 ms | 27.797 ms | 2.48x | 37.411 / 32.119 ms |
| GB300 | 10.801 ms | 27.978 ms | 2.59x | 34.041 / 32.439 ms |

This is one sample, not a stable throughput estimate or solver-accuracy parity
certificate. FPGS is faster in the captured physics graph, but **slower in this
sample's full environment wall time**. Do not compare these repaired timings
against the old dropped-row/warning-affected configurations as an optimization
gain. Repeated, accuracy-qualified rebaselining remains separate work.
The graph metric is the mean of 40 per-environment-step sums, each containing
four direct physics graph roots. See the independently recomputed
[capture audit](CHECKED_KEYBOARD4K_20260912.md) and its compact hashed evidence.

### Elliptic tasks: warning-free is not yet numerical acceptance

Allegro and ANYmal-D retain their original elliptic cone and 50 line-search
iterations. Separate traced controls use 16,384 worlds, seed 0, 200 warmup /
three repeats of 40 environment / 40 profile steps: 47,185,920 observed
world-solves per GPU. Explicit repeats matter: the Lab harness defaults to three.

| Task | Stock LS failures, RTX / GB300 | Shared search correction, RTX / GB300 |
| --- | ---: | ---: |
| Allegro | 9 / 4 | 6 / 8 |
| ANYmal-D | 1 / 2 | 0 / 0 |

Changing only Allegro's friction-cost difference, with the stock bracket,
removes all 9 / 4 warnings. Stable friction plus corrected bracket also records
zero warnings on both GPUs at the same exposure and unchanged task budgets.

All six paired controls have zero observed outer, row/contact-capacity and
nonfinite failures. This is not general numerical acceptance. The exact failure
rays expose different numerical issues:

- Allegro: on both stock rays, large frictionloss absolute costs swamp a small
  true improvement. Independent raw FP64 initial-step cost changes are
  -6.448e-6 / -2.868e-6; the GPU reports +3.419e-6 / +1.154e-5 instead. Replacing
  only frictionloss shifted-cost evaluation in CPU replay restores negative
  cost changes with unchanged derivatives. The paired stock-bracket GPU control
  confirms the causal correction, rather than relying on replay alone.
- ANYmal-D: there are no frictionloss rows. Near-cancellation in the prepared
  tangent-norm quadratic and its evaluation shifts the derivative root. Both
  raw-input FP64 roots and high-precision roots of the stored coefficients lie
  outside the saved GPU derivative-sign brackets. The FP32 alpha grid also
  cannot attain the nominal derivative threshold. Clearing the warning with
  adjacent-bracket termination does not independently repair the evaluator.

The opt-in helper now has separate immutable private factories for pyramidal
and elliptic Newton solvers. Only the elliptic factory adds stable Huber cost
differences; its derivatives, Hessians, prepared cone coefficients and inherited
ellipse evaluator are unchanged. Neither cone switching nor relaxed tolerances
are used. The generated pyramidal source is unchanged (`485f85cc...`).

Twenty-three repository-local CPU numerical/source tests pass, including real
native condim 3/4/6 contact cost and force, dense/CSR fused-Jv paths, all nine
Huber transitions, sub-ULP displacement and genuine budget exhaustion. The
whole benchmark tooling suite has 83 passing CPU tests after the new external
contact-prefix checker. These are numerical regression tests, not global
trajectory equivalence.

The [numerical report](ELLIPTIC_NUMERICS_20260912.md) documents the precision
limit and physical scale. Examined ANYmal corrected prepared-evaluator regrets
are below one full-objective FP32 ULP. More expensive direct residual evaluation
is retained as a diagnostic, not made mandatory without demonstrated physical
benefit. Conversely, ANYmal's largest GB stored-force-gradient peak increased
3.10x in the evolved rollout despite similar bulk percentiles. That tail is
disclosed, not dismissed or labeled uniformly improved.

### External contact-prefix guard

Native MJWarp warning bits alone miss a Newton-to-MJWarp conversion loss:
raw Newton contact demand can exceed the receiving MJ pool before native
constraint construction. A real three-contact / one-slot regression reproduced
truncation with all native warning bits zero. The existing count-snapshot kernel
now also latches negative raw counts, public Newton capacity overflow and MJ
pooled-capacity overflow into one persistent four-byte word. There is no new
per-step kernel or synchronization. Cached and zero-grid conversions are covered.

`SolverMuJoCo.contact_capacity_status()` and `check_contact_capacity()` expose
this history outside capture; only explicit acknowledgment clears it. Checked
captures require these APIs as well as native warning checks. Native MJ collision
records this external-converter check as not applicable, not as native capacity
evidence. As with other sticky checks, rejection cannot repair an affected state.

## Reproduction entrypoint

The retained `manifest.json` in each evidence directory contains exact paired
diagnostic commands, source pins and hardware UUIDs. Diagnostic captures are
not timing samples. For an actual checked Keyboard4K comparison, use the
Newton-owned driver below with the existing Lab checkout; choose a fresh
output directory. The driver uses seed0, while the held-out calibration above
uses seed1. It rechecks all supported sticky flags and refuses affected results.

```sh
uv run --no-project --python /path/to/isaaclab/.venv/bin/python \
  python tools/fpgs_bench/compare_backends.py \
  --isaaclab /path/to/isaaclab --fpgs /path/to/newton --mjwarp /path/to/newton \
  --gpus 0 1 --task keyboard-so101 --num-envs 4096 \
  --warmup-steps 200 --steps 1000 --profile-steps 40 --repeats 3 \
  --check-overflow --mjwarp-linesearch-fix \
  --capacity keyboard-so101:fpgs:dense_max_constraints=704 \
  --capacity keyboard-so101:fpgs:rigid_contact_max=147456 \
  --capacity keyboard-so101:fpgs:broad_phase_output_max=57344 \
  --capacity keyboard-so101:mjwarp:njmax=320 \
  --capacity keyboard-so101:mjwarp:nconmax=32 \
  --capacity keyboard-so101:mjwarp:rigid_contact_max=131072 \
  --capacity keyboard-so101:mjwarp:broad_phase_output_max=45056 \
  --output-dir /path/outside/checkouts/keyboard-checked
```

The example is scoped to Keyboard's calibrated 4K workload. The experimental
helper also supports the original elliptic Newton recipes; do not change their
cones. Other world counts and tasks require their own measured capacities.

`njmax` is per-world; `nconmax` is a pooled reservation multiplied by world
count. Never confuse pooled average reservation with a hard per-world limit.
Do not shrink sparse Jacobian storage independently without measuring its
unclamped transient demand.

## Local evidence index

- `/tmp/fpgs-capacity640-20260911-01`
- `/tmp/fpgs-capacity640-contact196608-seed1-20260911-01`
- `/tmp/fpgs-capacity704-contact147456-seed2-20260912-02`
- `/tmp/fpgs-status-gpu-tests-20260912-01`
- `/tmp/fpgs-capacity-gpu-tests-20260912-02`
- `/tmp/fpgs-broad57344-seed3-20260912-02`
- `/tmp/mj-capacity-stock-kuka64-20260911-03`
- `/tmp/mj-capacity-nonincremental-kuka64-20260911-01`
- `/tmp/mj-capacity-ray-kuka64-20260912-01`
- `/tmp/mj-zero-helper-kuka64-20260912-01`
- `/tmp/mj-capacity-keyboard4096-20260912-01`
- `/tmp/mj-adjacent-kuka64-20260912-01`
- `/tmp/mj-adjacent-keyboard64-20260912-01`
- `/tmp/mj-stock-franka64-20260912-02`
- `/tmp/mj-adjacent-franka64-20260912-01`
- `/tmp/mj-capacity-full-keyboard64-20260912-01`
- `/tmp/mj-capacity-full-keyboard4096-20260912-01`
- `/tmp/fpgs-checked-keyboard64-20260912-01`: actual checked production entrypoint,
  Nsight export/analyzer and parent acceptance pass for both backends on both
  GPUs; 64 worlds, 10 warmup + 2 environment + 2 profile steps, seed 0. This is
  an integration smoke using historical adjacent-only helper `a342`, not final
  `c5f0a6ae`; it is not a representative throughput claim. Actual capacities
  are FPGS704 / MJ320 rows, 3,072 contacts (`nconmax=48`), 1,024 broad output.
- `/tmp/fpgs-keyboard-fixed-root-capacity-9otnbu`: two original-source CPU tests
  proving fixed-root/108-descendant response mapping and 205-contact allocator
  controls (zero fixed-case rows versus 615 responsive-contact rows).
- `/tmp/fpgs-response-gpu-tests-20260912-01`: 83 tests per GPU, no skips/failures.
- `/tmp/fpgs-mj-response-filter-9B3Y5J`: response-filter regression-before evidence.
- `/tmp/mj-nudge-franka64-20260912-01`: isolated rounding-step nudge retains
  all three failures per GPU; a proposal fix alone does not repair insertion.
- `/tmp/fpgs-mj-bracket-acquisition-b5zxcD`: independently executed native
  convex counterexample and exact source pins.
- `/tmp/mj-response-census-keyboard64-20260912-01`
- `/tmp/mj-collapsed-franka64-20260912-01`
- `/tmp/mj-collapsed-response-keyboard4096-20260912-01`
- `/tmp/mj-collapsed-kuka16384-20260912-01`: rejected64-row calibration;
  see its separate `acceptance.json`, not just diagnostic completion.
- `/tmp/mj-collapsed-kuka16384-cap84-20260912-01`: accepted warning/capacity/census
  diagnostic, not timing; 162,529,280 observed world-solves per GPU.
- `/tmp/fpgs-checked-keyboard4096-20260912-01`: final-helper actual checked
  capture path, both backends and GPUs; one qualified timing sample above.
- `/tmp/mj-collapsed-franka16384-cap40-20260912-01`: rejected row-overflow run.
- `/tmp/mj-collapsed-franka16384-cap56-20260912-01`: failed before physics,
  diagnostic sibling import; not a numerical result.
- `/tmp/mj-collapsed-franka16384-cap56-20260912-02`: accepted observed
  warning/capacity diagnostic at 56 rows, explicit repeats three.
- `/tmp/mj-native-allegro16384-cap112-ncon22-seed1-20260912-01`
- `/tmp/mj-elliptic-anymald16384-cap32-seed1-20260912-01`
- `/tmp/fpgs-capacity-v3-gpu-tests-20260912-01`: all 88 tests pass on each GPU
  at clean `108459ec`, no skips; final source and idle guards pass.
- Diagnostic sources, source pins, replay tests and control plan:
  `/tmp/fpgs-capacity-20260911-O7dqFC`.

Failed instrumentation attempts are preserved, not counted as solver results:
an initial allocation-inventory traversal exceeded its object budget; an early
MJ observer kernel needed explicit float initialization for Warp loop typing;
one command used an incorrect task identifier. Corrected controls above passed
their ownership/source checks.

## Acceptance still required

1. Establish broader physical numerical acceptance separately from the completed
   same-input numerical regressions and warning/capacity observations.
2. Recheck calibrated pools if the final numerical correction changes
   trajectories; retain full-run raw demand checks, not boundary counts alone.
3. Rebaseline only validated configurations using uninstrumented paired timing;
   do not label repaired dropped physics as an optimization speedup.
