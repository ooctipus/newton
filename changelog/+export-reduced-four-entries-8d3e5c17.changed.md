The fast-mode export of globally reduced contacts handles four hashtable entries per 32-lane block with one lane per value slot: each lane loads its slot's winner once and the roundoff-duplicate rule is decided from the seven records exchanged through warp shuffles, instead of 21 pair lanes re-loading two records each and a single lane exporting afterwards. The exported contacts are unchanged; the CPU and deterministic paths keep the one-entry-per-block form.
