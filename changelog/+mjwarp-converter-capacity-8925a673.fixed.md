- Add persistent external-contact prefix checks to `SolverMuJoCo` and require them in checked benchmark captures, catching Newton-to-MJWarp truncation even when native MJWarp warning flags remain zero. The existing count-snapshot kernel latches negative counts and demand exceeding public Newton or pooled MJWarp contact capacity; only an explicit host acknowledgment clears this history.
