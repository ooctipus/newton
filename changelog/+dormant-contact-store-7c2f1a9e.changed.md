Replace the SDF contact replay cache behind `CollisionPipeline(sdf_contact_replay_max=...)` with a per-dynamic-shape dormant contact store: contacts between a sleeping dynamic shape and an immovable shape stay out of the live `Contacts` buffer (exposed as `Contacts.dormant_contact_store`), unchanged sleeping dynamic-kinematic SDF pairs are still skipped exactly, and `SolverMuJoCo` injects the stored rows in the substep a tree wakes. The new `sdf_contact_slab_rows` keyword (default 256) sizes the per-shape capacity; the store requires `contact_matching="disabled"`. Contact conversion and wake injection in `SolverMuJoCo` now grid-stride over the live contact count instead of the buffer capacity.
