Fix `SolverMuJoCo` external Newton contacts so velocity-retained speculative candidates activate as per-substep normal barriers instead of remaining force-free until overlap. Reject candidate raw-mesh overlap and stationary-target pure-translation transit between collision refreshes for strict mesh pairs, using a fixed-cost swept-bounds certificate before raw-triangle traversal. Commit a dyadically certified tolerance-valid prefix after constant-twist sweep failures instead of holding a reference pose already inside the accepted separation tolerance. Make `CollisionPipeline.check_mesh_nonpenetration` fail closed when an eligible raw-mesh domain has zero mesh-pair capacity. Reject the RK4 integrator because its intermediate stages cannot refresh external contact geometry; use Euler, implicit, or implicit-fast instead. This mode is not general continuous collision detection.
